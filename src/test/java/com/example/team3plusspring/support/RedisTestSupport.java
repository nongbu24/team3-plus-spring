package com.example.team3plusspring.support;

/**
 * RedissonClient를 사용하는 테스트는 Docker Compose의 Redis 컨테이너가 실행된 상태에서 동작한다.
 *
 * macOS/Linux: docker compose up -d redis
 * Windows PowerShell: docker compose up -d redis
 */
public abstract class RedisTestSupport {
}
